let alumnos = []

async function loadAlumnos()
    try / catch
    clearMessage()
    await fetch API_URL, OPTIONS
    if throw new error


async function createAlumnos(data)


form addEventListener("submit",)

Entidad principal: alumnos
Cada alumno debe tener la siguiente estructura mínima:
{
  "id": 1,
  "nombre": "Ana",
  "apellido": "Gomez",
  "edad": 20,
  "email": "ana@gmail.com",
  "comision": "A",
  "activo": true
}

renderRows()
    cambiar total-alumnos
    si 0 ? mostrar no alumnos cargados : mostrar alumnos con .map

validateAlumno()

///

HTML: 
- total-alumnos / span
- td, tr
